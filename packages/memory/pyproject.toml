[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cortex-memory"
version = "0.1.0"
description = "Long-term memory module for Cortex — semantic search + LLM extraction"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Zhe Wang" }]
dependencies = [
    "numpy",
    "anthropic",
    "mcp",
]

[project.scripts]
memory = "memory.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["memory"]
